apiVersion: v1
## This test example contains an approval step, which should be valid if it has
# type: approval even with no image field
steps:
  - name: step-1
    image: my-image
  - name: approval-step
    type: approval